# Core runtime
discord.py>=2.4,<3.0
aiohttp>=3.9,<4.0
SQLAlchemy>=2.0,<3.0
python-dotenv>=1.0,<2.0

# Database drivers
aiosqlite>=0.20          # local development (SQLite)
# asyncpg>=0.29          # production (PostgreSQL / Supabase) — uncomment for prod

# Development / tests
pytest>=8.0
pytest-asyncio>=0.23
